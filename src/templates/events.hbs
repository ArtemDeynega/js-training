{{!-- <template id="menu-template">
    <div class="menu">
        <li>
            <h2>{{}}Заголовок{{}}</h2>
            <p>Локация</p>
            <p>Время старта</p>
            <p>Спикер</p>
        </li>
    </div>
</template> --}}

    <template id="menu-template">
        <div class="menu">
            <h3 class="menu-title">{{title}}</h3>
            <ul class="menu-list">
                {{#each items}}
                <li class="menu-item">{{this}}</li>
                {{/each}}
            </ul>
        </div>
    </template>